{
  box-sizing: border-box;
  font-family: 'Segoe UI', Tahoma, sans-serif;
}

body {
  margin: 0;
  background: #f4f6fb;
  color: #333;
}

header {
  background: linear-gradient(90deg, #0078d7, #005bb5);
  color: white;
  text-align: center;
  padding: 25px 0;
  font-size: 28px;
  font-weight: 600;
  letter-spacing: 1px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.15);
}

.main-options {
  display: flex;
  justify-content: center;
  gap: 25px;
  padding: 50px 20px;
  flex-wrap: wrap;
}

.card {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 20px;
  padding: 40px 30px;
  text-align: center;
  width: 320px;
  box-shadow: 0 8px 16px rgba(0,0,0,0.1);
  cursor: pointer;
  transition: 0.3s ease;
  backdrop-filter: blur(8px);
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 20px rgba(0,0,0,0.15);
}

.card img {
  width: 70px;
  margin-bottom: 15px;
}

.card h2 {
  color: #0078d7;
  margin: 10px 0;
}

.card p {
  color: #555;
}

.hidden {
  display: none;
}

.progress-bar {
  width: 80%;
  height: 15px;
  background-color: #ddd;
  margin: 20px auto;
  border-radius: 8px;
  overflow: hidden;
}
/* ===== Neon Loading & Sparkle Effects ===== */
#loading-section {
  text-align: center;
  margin: 40px 0;
}

.loading-bar {
  width: 60%;
  margin: 25px auto;
  height: 10px;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 5px;
  overflow: hidden;
  box-shadow: 0 0 15px purple;
}

.loading-progress {
  width: 0;
  height: 100%;
  background: linear-gradient(90deg, purple, violet, white);
  animation: loadingAnim 5s linear forwards;
}

@keyframes loadingAnim {
  from { width: 0%; }
  to { width: 100%; }
}

.sparkle-container {
  display: flex;
  justify-content: center;
  gap: 8px;
}

.sparkle {
  width: 10px;
  height: 10px;
  background: white;
  border-radius: 50%;
  box-shadow: 0 0 12px purple;
  animation: sparkleAnim 1.5s infinite alternate;
}

@keyframes sparkleAnim {
  from { opacity: 0.3; transform: scale(0.8); }
  to { opacity: 1; transform: scale(1.3); }
}

#loading-text {
  color: white;
  font-weight: 600;
  text-shadow: 0 0 10px purple;
  font-size: 1.1em;
  margin-top: 10px;
}


#progress {
  height: 100%;
  width: 0%;
  background-color: #0078d7;
  transition: width 0.3s;
}

.nav-btn, .cancel-btn {
  background: #0078d7;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  margin: 10px;
}

.nav-btn:hover, .cancel-btn:hover {
  background: #005bb5;
}

.converted-files {
  padding: 30px 40px;
}

.converted-files h2 {
  color: #0078d7;
  margin-bottom: 15px;
}

.gallery {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
}

.file-item {
  display: flex;
  align-items: center;
  background-color: white;
  padding: 12px 15px;
  border-radius: 10px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  width: 220px;
  transition: 0.2s ease;
}

.file-item:hover {
  transform: scale(1.03);
}

.file-item img {
  width: 30px;
  margin-right: 10px;
}

footer {
  text-align: center;
  padding: 15px;
  background-color: #0078d7;
  color: white;
  font-size: 14px;
  margin-top: 30px;
}
